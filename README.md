# Bryan's Portfolio Website

WIP

## Development & Deployment Workflow

1. **Development mode** – run the local development server to see changes live:

```bash
npm run dev
```

2. **Build** – compile the project and generate the production-ready static files:

```bash
npm run build
```

3. **Deploy** – Build the project and publish it to GitHub Pages

```bash
npm run deploy
```
